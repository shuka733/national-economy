# Improvement Test Checklist

## 1. 属性マークの表示

- 手札・自分の建物・公共の場・カードプレビューで、属性表示がアイコンのみになっていること
- `工場` / `農園` / `売却不可` の文字ラベルが属性バッジ内に表示されないこと
- 効果文中の `[農園マーク]` / `[工場マーク]` / `[売却不可マーク]` がアイコン表示になること
- 既存カードの効果文で、数値強調や通貨強調が崩れていないこと

## 2. 果樹園の配置制限

- 手札 3 枚以下のとき、`果樹園` にワーカーを配置できること
- 手札 4 枚以上のとき、`果樹園` にワーカーを配置できないこと
- CPU が手札 4 枚以上で `果樹園` を選ばないこと
- P2P / ローカル対戦の両方で同じ制限になること

## 3. 和風テーマ背景

- タイトル画面で `和風` を選ぶと、背景画像が切り替わること
- ゲーム画面で `和風` を選ぶと、盤面背景が切り替わること
- `default` / `steampunk` / `watercolor` に切り替えても、背景画像が正しく戻ること
- ページ再読込後も現在テーマの背景が崩れないこと

## 4. にこいち建設の導線

- `にこいち建設` 発動時に、全面ポップアップではなく手札上部のインライン UI が表示されること
- 1 枚目を選ぶと、同コストのカードだけが 2 枚目候補として選択可能になること
- 条件を満たさないカードは選択できないこと
- 2 枚選択後に `確定` が押せること
- `キャンセル` で通常状態に戻ること
- 相手プレイヤーは全面モーダルでロックされず、盤面閲覧を継続できること

## 5. P2P 再接続

- ホストが生存中に、ゲストがブラウザ再起動 / タスクキル後に同じルーム ID へ再接続できること
- 再接続したゲストに、元の `playerID` と表示名が復元されること
- 再接続後に現在のゲーム状態が同期されること
- 進行中ゲームで、別の新規ゲストが既存ゲストの枠を奪えないこと
- 同一セッショントークンが重複接続した場合は拒否されること

## 6. 回帰確認

- `npm run build` が通ること
- ローカル対戦が開始できること
- P2P のホスト作成 / 参加 / ゲーム開始ができること
- スマホ版でカード表示と長押しプレビューが崩れていないこと
- フルスクリーン導線や既存のメニュー遷移に影響が出ていないこと
